version: '3.8'

services:
  severus:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: severus-ai-app
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - HOST=0.0.0.0
      - PORT=8000
    restart: unless-stopped
